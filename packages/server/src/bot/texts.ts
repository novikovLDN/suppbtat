import { config } from '../config.js';
import { ticketNumber } from '../services/serializers.js';

const brand = config.brand.name;

export const t = {
  welcome: (firstName: string) =>
    `Привет, ${firstName} 👋\n` +
    `Тебя приветствует служба поддержки ${brand}.\n\n` +
    `Чтобы мы помогли — нажми кнопку «🆘 Написать в поддержку» ниже и опиши проблему по шаблону ✅\n\n` +
    `|- Что не работает?\n` +
    `|- На каком устройстве?\n` +
    `|- Wi-Fi или мобильный интернет?\n` +
    `|- Когда появилась проблема?\n` +
    `|- Текст ошибки (если есть)\n\n` +
    `📷 Можно приложить скриншоты — это сильно ускорит решение.`,

  help:
    `❓ <b>Помощь по использованию бота</b>\n\n` +
    `🛠 <b>Основные функции:</b>\n` +
    `• Создание тикетов поддержки\n` +
    `• Просмотр истории обращений\n` +
    `• Дополнение активного тикета (текст + фото)\n` +
    `• Получение информации о вашем обращении\n\n` +
    `💬 <b>Как писать в поддержку:</b>\n` +
    `1. Просто отправьте сообщение с описанием проблемы\n` +
    `2. Бот автоматически создаст тикет\n` +
    `3. Все последующие сообщения добавятся в тот же тикет\n` +
    `4. Можно прикладывать фото и скриншоты 📷\n\n` +
    `⚡️ <b>Команды:</b>\n` +
    `/start — главное меню\n` +
    `/help — эта справка\n` +
    `/tickets — ваши тикеты\n\n` +
    `🆘 Если что-то не работает, попробуйте /start`,

  contactPrompt:
    `💬 <b>Написать в поддержку</b>\n\n` +
    `Опишите вашу проблему максимально подробно:\n` +
    `• На каком устройстве возникла проблема\n` +
    `• Что именно не работает\n` +
    `• Когда началась проблема\n` +
    `• Приложите скриншоты, если возможно 📷\n\n` +
    `✍️ <i>Напишите ваше сообщение — я создам тикет автоматически.</i>`,

  supplementPrompt: (ticketId: number) =>
    `📝 <b>Дополнить тикет #${ticketNumber(ticketId)}</b>\n\n` +
    `Добавьте новую информацию, которая поможет оператору:\n` +
    `• Что изменилось с момента обращения\n` +
    `• Дополнительные детали или шаги, которые вы пробовали\n` +
    `• Свежие скриншоты или фото ошибки 📷\n\n` +
    `✍️ <i>Просто отправьте текст и/или фото — всё попадёт в этот тикет.</i>`,

  ticketCreated: (ticketId: number) =>
    `✅ <b>Ваш запрос получен!</b>\n` +
    `Специалист технической поддержки свяжется с вами в ближайшее время.\n\n` +
    `🎫 <b>Номер тикета:</b> #${ticketNumber(ticketId)}\n` +
    `📝 <b>Статус:</b> Открыт\n\n` +
    `💡 <i>Можете продолжать писать в этот чат — все сообщения будут переданы в поддержку.</i>`,

  appended: (ticketId: number) =>
    `📨 <i>Добавлено в тикет #${ticketNumber(ticketId)}. Оператор увидит ваше сообщение.</i>`,

  supplementDone: (ticketId: number) =>
    `✅ <b>Тикет #${ticketNumber(ticketId)} дополнен.</b>\n\n` +
    `Ваша информация передана оператору. Спасибо!`,

  alreadySupplemented: (ticketId: number) =>
    `ℹ️ <b>Тикет #${ticketNumber(ticketId)} уже был дополнен.</b>\n\n` +
    `Повторное дополнение недоступно. Если нужно — дождитесь ответа оператора в этом чате.`,

  adminPanel: (url: string) =>
    `🛡 <b>Панель администратора Atlas Secure</b>\n\n` +
    `Нажмите кнопку ниже, чтобы войти в дашборд поддержки.\n` +
    `Вам будут доступны все чаты и управление операторами.\n\n` +
    `🔗 Ссылка действует ограниченное время и предназначена только для вас.`,

  adminPanelNoUrl: (token: string) =>
    `🛡 <b>Панель администратора Atlas Secure</b>\n\n` +
    `Публичный URL не настроен (PUBLIC_URL). Откройте дашборд и вставьте этот токен ` +
    `в адрес: <code>/?login=ТОКЕН</code>\n\n` +
    `<code>${token}</code>`,

  restingNotice:
    `⛔️ <b>Поддержка сейчас отдыхает</b>\n\n` +
    `Мы ответим вам в рабочее время.\n` +
    `Пожалуйста, оставьте сообщение — и мы вернёмся к вам как можно скорее.`,

  ticketsHeaderEmpty:
    `📋 <b>Ваши тикеты</b>\n\n` +
    `📝 У вас нет активных тикетов.\n\n` +
    `💬 <i>Нажмите «🆘 Написать в поддержку», чтобы создать новый тикет.</i>`,

  ticketsHeaderActive: (ticketId: number) =>
    `📋 <b>Ваши тикеты</b>\n\n` +
    `🎫 <b>Активный тикет:</b> #${ticketNumber(ticketId)}\n` +
    `📊 <b>Статус:</b> Открыт\n\n` +
    `💬 <i>У вас есть активный тикет. Все новые сообщения будут добавлены к нему.</i>`,

  ticketClosedByUser: (ticketId: number) =>
    `🔒 <b>Тикет #${ticketNumber(ticketId)} закрыт.</b>\n\n` +
    `Спасибо за обращение! Если возникнет новый вопрос — нажмите ` +
    `«🆘 Написать в поддержку», и я создам новый тикет.`,

  ticketClosedByOperator: (ticketId: number) =>
    `🔒 <b>Тикет #${ticketNumber(ticketId)} закрыт оператором.</b>\n\n` +
    `Надеемся, мы помогли! Если вопрос остался — нажмите ` +
    `«🆘 Написать в поддержку», чтобы открыть новый тикет.`,

  operatorJoined: (ticketId: number, name: string) =>
    `👨‍💻 <b>${name}</b> из поддержки взял ваш тикет #${ticketNumber(ticketId)} в работу.\n` +
    `Опишите, пожалуйста, детали — мы уже на связи.`,
};

export function ticketStatusLabel(status: 'OPEN' | 'CLOSED'): string {
  return status === 'OPEN' ? '🟢 Открыт' : '🔒 Закрыт';
}
